<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network access -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Required to show the persistent notification that keeps WorkerService alive -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Required on API 34+ when foregroundServiceType="dataSync" -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Android 13+ runtime notification permission -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Nativetest"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <!--
            Grants the system linker namespace access to the device vendor's OpenCL
            library so that our native dlopen("libOpenCL.so") succeeds on Android 10+.
            android:required="false" keeps the app installable on devices without OpenCL.
            Must be a direct child of <application>, not <manifest>.
        -->
        <uses-native-library android:name="libOpenCL.so" android:required="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Foreground service: keeps worker threads alive when app is backgrounded -->
        <service
            android:name=".WorkerService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
        <!-- FOREGROUND_SERVICE_DATA_SYNC is declared above; required on API 34+ -->
    </application>



</manifest>